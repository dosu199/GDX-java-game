package io.github.some_example_name;

public class Enemy extends Entity {
    public Enemy(String textureLocation, V2 p, V2 scale) {
        super(textureLocation, p, scale);
    }

    public void update() {
    }
}
